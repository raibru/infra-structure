---
# defaults file for setup.server.nfs

nfs_package: "{{ __nfs_package }}"
nfs_service: "{{ __nfs_package }}"
nfs_artifacts_dirs: "{{ __nfs_artifacts_dirs }}"
nfs_export_dir: /mnt/nfs
nfs_export_params: "*(rw,all_squash,insecure,async,no_subtree_check,anonuid=1000,anongid=1000)"

