galaxy_info:
  author: raibru
  description: install NFS client tool on worker hosts and mount them under self created inventory names with some artifact directories 
  company:

  license: license (MIT)

  min_ansible_version: 2.1

  platforms:
    - name: Ubuntu
      versions:
      - 20.04
  galaxy_tags: []

dependencies: []


