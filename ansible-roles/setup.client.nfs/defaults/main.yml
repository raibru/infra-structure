---
# defaults file for setup.client.nfs

nfs_package: "{{ __nfs_package }}"
nfs_server_share_point: "192.168.59.11:/mnt/nfs"
nfs_remote_mount_point: "{{ nfs_server_share_point}}/{{ inventory_hostname_short }}"
nfs_mount_dir: /mnt/nfs

nfs_client_dirs:
  - "/mnt/nfs/wireshark"
  - "/mnt/nfs/logs"
  - "/mnt/nfs/session"
  - "/mnt/nfs/archive"
